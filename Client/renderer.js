const myForms = document.querySelectorAll('.myForm');

myForms.forEach(form=>form.addEventListener("submit",()=>{
    console.log("Working")
}))


function postCardCreate (pTitle, pContent) {
    const card = ``
} 